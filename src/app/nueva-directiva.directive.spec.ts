import { NuevaDirectivaDirective } from './nueva-directiva.directive';

describe('NuevaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new NuevaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
