import { ModificadorPipe } from './modificador.pipe';

describe('ModificadorPipe', () => {
  it('create an instance', () => {
    const pipe = new ModificadorPipe();
    expect(pipe).toBeTruthy();
  });
});
